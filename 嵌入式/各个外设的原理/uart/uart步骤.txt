一.设置引脚用于串口。并将该引脚设置为内部上拉
二.设置uart串口传输格式(例如：使用多少数据位，通常是8位。是否使用校验位。几位停止位等)。
三.设置波特率。公式：UBRDIVn = (int)( UART clock / ( buad rate x 16) ) –1
    1.设置UART clock时钟来源，这里选用PCLK。以及设置中断方式等(中断的实现步骤在中断部分)
    2.设置UBRDIVn
四.由于这里暂时先不用FIFO等其他功能，所以先用默认值